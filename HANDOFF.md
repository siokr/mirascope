# mirascope 项目交接说明

> 更新时间：2026-07-29
> 当前开发分支：`codex/m01-source-relocation`
> 分支基线：`63bea7a`
> 本地 `main`：`63bea7a`，尚未自动推送

## 1. 现在做什么

当前正在实施 `M01-015 文件变化与重新定位`。

目标是让已导入小说的原 TXT 被移动、删除或修改后，仍然保留媒体、章节派生文本、阅读设置和阅读进度，并允许用户安全地重新选择来源：

```text
发现原文件不可用
→ 选择新文件
→ 重新计算指纹
→ 指纹相同：更新来源路径
→ 指纹不同：要求明确确认，不静默替换
```

当前分支刚建立，尚未提交 M01-015 的设计或实现。

## 2. 已经完成了什么

### 已进入 `main`

- M01-001：工程基础决策；
- M01-004：日志与统一错误模型；
- M01-005：本地数据库与 schema v1/v2 迁移；
- M01-006：媒体库；
- M01-007：TXT 文件选择、校验、流式 SHA-256 指纹和重复检测；
- M01-008：UTF-8、UTF-16、GB18030 解码和文本规范化；
- M01-009：中英文章节识别与无章节回退；
- M01-010：TXT 原子导入、派生文本提升、失败补偿和编码持久化；
- M01-011：小说详情与目录；
- M01-012：纵向滚动阅读器；
- M01-013：全局与作品级阅读设置；
- M01-014：语义进度持久化、revision 冲突保护和恢复。

### 最近一次当次验证

在 `63bea7a` 上重新执行：

```text
格式检查：127 个文件，0 变更
静态分析：0 问题
完整测试：182/182 通过
```

由于 Flutter 的 Windows 批处理包装层曾被残留进程卡住，本次分析和测试使用同一 Flutter SDK 的 `flutter_tools.snapshot` 直接执行。测试逻辑与常规 `flutter analyze/test` 相同。

### 当前可用主链路

代码层已具备：

```text
文件选择
→ 文件校验和指纹
→ 编码识别与规范化
→ 章节识别
→ schema v2 原子导入
→ 媒体库
→ 小说详情
→ 阅读器
→ 阅读设置
→ 阅读进度恢复
```

其中平台交互与真实 Windows 窗口仍未完成人工验收。

## 3. 卡在哪里

### Windows MSVC 工具链

本机 Windows 构建在 CMake 识别 MSVC 时误选 `HostX86 → x64` 并卡死；最小编译使用 `HostX64 → x64` 可工作。

因此以下任务虽然代码和自动化测试已完成，仍在任务清单中标记为 `blocked`：

- M01-002、M01-003；
- M01-007、M01-008；
- M01-010 至 M01-014。

不能把自动化测试通过描述为 Windows 实机已验证。

### Flutter 包装脚本可能残留挂起进程

如果 `dart --version`、`flutter analyze` 连续无输出：

1. 先检查是否有本轮残留的 `dart`、`flutter`、`pwsh` 进程；
2. 只终止明确属于本轮挂起命令的进程；
3. 不要粗暴清空 Flutter 缓存；
4. 必要时用 SDK 中的 `dart.exe + flutter_tools.snapshot` 执行分析和测试；
5. SDK 锁文件位于 `D:\dev\flutter\bin\cache`，只有确认无人持有后才能删除陈旧锁。

### M01-015 尚缺仓储能力

现有 `ImportRepository` 只有：

- 按指纹查已完成记录；
- 提交成功导入；
- 记录失败。

仍需增加：

- 按媒体 ID 查当前有效来源；
- 将不可用来源标记为 `missing`；
- 指纹相同时原子更新路径、大小和修改时间；
- 指纹不同时保持数据库不变并返回确认状态。

## 4. 下一步做什么

### M01-015 实施顺序

1. 编写设计和实施计划；
2. 扩展 `ImportRepository` 契约和 Drift 实现；
3. 实现重新定位应用用例；
4. 复用 M01-007 的 TXT 选择与指纹检查；
5. 指纹相同则恢复来源关联；
6. 指纹不同则返回明确确认结果，默认不写数据库；
7. 接入小说详情页的“重新定位源文件”按钮；
8. 刷新详情状态并保留阅读进度；
9. 覆盖取消、不存在、相同指纹、不同指纹、写入失败和并发状态；
10. 执行全量质量门槛。

如要支持用户确认后的内容变化更新，必须先生成全新解析结果，再在事务中替换章节与来源；失败时继续保留旧派生文本、旧章节和旧进度。不得先破坏旧版本再尝试解析。

### M01-015 之后

- M01-016：自动化质量门槛与 CI；
- M01-017：小型、常规、50 MB TXT 性能基线；
- M01-018：文档、演示、已知问题和 MVP 收口；
- Windows 工具链恢复后补全部实机验收。

## 5. 哪些坑不要踩

### 数据安全

- 不修改、移动或删除用户原文件；
- 不因源文件丢失而删除媒体、章节、进度或设置；
- 指纹不同绝不静默替换；
- 重新解析失败必须保留旧的可读版本；
- 不用文件名或路径判断文件身份；
- 不绕过 Repository 分别修改关联表。

### 隐私与日志

- 不记录完整路径、文件名、标题、正文、原始字节、异常或堆栈；
- 日志只使用稳定错误码、固定阶段、大小和耗时；
- 测试不提交商业小说或真实私人路径。

### 编码与解析

- 不使用系统默认编码；
- 不允许 malformed 解码或静默替换字符；
- 不概率猜测后直接采用编码；
- 不重新定义 M01-009 的章节规则；
- 不把编码塞入无关字段；schema v2 已有 `text_encoding`。

### 界面和架构

- 页面不直接访问文件系统或 Drift；
- `domain/application` 不依赖平台插件或 Drift；
- 平台适配留在 data 层；
- 不展示无法完成的假操作；
- 相同条目的重复重新定位操作必须防重入；
- 取消选择不是错误。

### Git 和验证

- 当前开发应基于 `codex/m01-source-relocation`；
- 合并使用 `--ff-only`，除非真实分叉已经审查；
- 不自动推送，除非用户明确要求；
- 不把旧测试数字复制为本次证据；
- 生成文件只有真实内容变化时才提交；
- Windows 换行可能造成无内容差异的脏状态，先看 `git diff`；
- Drift 与 `drift_dev` 保持兼容版本。

## 6. 建议阅读顺序

1. `HANDOFF.md`
2. `docs/mvp-task-list.md`
3. `docs/novel-reader-spec.md`
4. `docs/data-model.md`
5. M01-010 至 M01-014 的设计文档
6. `lib/src/features/importing/`
7. `lib/src/features/novel/`
8. 对应的 `test/src/features/` 测试

## 7. 一句话状态

MVP 的 TXT 导入、阅读、设置和进度恢复代码链路已形成；当前正在补文件移动、丢失和内容变化后的安全重新定位，最大外部阻塞仍是 Windows MSVC 工具链。
