# mirascope 项目计划

## 1. 项目定位

`mirascope` 是一个本地优先的个人阅读与媒体管理应用，也是用于求职展示和 GitHub 交流学习的长期项目。

长期愿景是统一管理小说、漫画和番剧；近期目标不是同时铺开所有媒介和技术栈，而是先完成一个真实可用、可测试、可公开演示的小说阅读产品。

项目服务三个场景：

- 作者日常管理和阅读自己的本地内容；
- 求职时展示完整产品思考、Flutter 工程能力和技术取舍；
- 在 GitHub 公开代码、设计与演进过程。

## 2. 产品原则

- **阅读体验优先**：先形成完整主链路，再增加媒介和云能力。
- **本地优先**：阅读、进度和设置不依赖网络。
- **真实状态优先**：计划中的能力不得写成已经支持。
- **渐进架构**：仅在当前版本确有需要时引入服务端、Rust 或新平台。
- **可验证交付**：完成标准必须能由测试、构建、基准或演示证明。
- **单人可维护**：范围和技术成本必须符合个人长期开发条件。

## 3. 目标用户与近期非目标

### 3.1 目标用户

- 管理和阅读本地 TXT、EPUB 小说的桌面或 Android 用户；
- 重视离线阅读、进度恢复和数据可控性的用户；
- 希望参考 Flutter 中型项目演进过程的开发者。

### 3.2 近期非目标

- 社区、评论、弹幕和重运营；
- 推荐、AI、商业化；
- 在线漫画或番剧内容聚合；
- DRM 内容处理和云端原始内容托管；
- 全平台同时发布。

## 4. 权威版本路线

本节是版本范围的单一信息源。其他文档只引用这里的范围，不重新扩大或缩小边界。

### 4.1 MVP 0.1：Windows 本地 TXT 阅读闭环

**目标**

完成第一个能日常使用和公开演示的本地小说闭环，验证工程骨架、本地数据、TXT 导入和进度恢复。

**包含**

- Flutter 应用骨架、路由、主题、日志和统一错误处理；
- Windows 主平台；
- 本地媒体库；
- TXT 导入、编码识别和章节识别；
- 小说详情、章节目录和纵向滚动阅读器；
- 字号、行距和基础主题；
- 阅读进度持久化和异常终止恢复；
- 核心单元测试、数据层测试和关键 Widget 测试；
- 可复现运行说明和演示证据。

**不包含**

- EPUB 和 Android 正式支持；
- 漫画、登录与同步；
- 下载、番剧和在线内容源；
- Go、PostgreSQL 和 Rust 模块。

**核心演示路径**

```text
导入 TXT -> 加入媒体库 -> 打开小说 -> 切换章节
-> 退出或终止应用 -> 再次启动 -> 恢复阅读位置
```

**完成标准**

- 核心演示路径在 Windows 上完整通过；
- 编码、异常文件和重复导入行为符合小说规格；
- 数据迁移、进度恢复和关键领域规则有自动化测试；
- `flutter analyze`、`flutter test` 和 Windows 构建达到质量门槛；
- README、运行说明、演示和已知问题与实际状态一致。

### 4.2 MVP 0.2：可公开展示的小说阅读器

**进入条件**

MVP 0.1 已发布，真实使用中没有阻塞阅读或导致数据丢失的问题。

**包含**

- Android 主流程适配；
- EPUB 导入和基础兼容；
- 书签；
- 阅读器排版、导航和无障碍细节完善；
- 大文件、异常文件、内存和启动性能基准；
- Windows 与 Android 演示包；
- GitHub Release、截图或短演示、变更记录和已知问题。

**不包含**

- 漫画、在线内容源、账号与同步；
- 下载和番剧；
- Rust，除非基准证明 Dart 无法满足已经冻结的指标。

**完成标准**

- Windows 和 Android 均通过小说核心演示路径；
- EPUB 支持边界和失败行为有样例验证；
- 发布物可按文档步骤复现；
- 性能结论来自可重复基准。

### 4.3 Version 0.3：本地漫画阅读

**进入条件**

小说阅读器已达到稳定展示状态，统一媒体模型能够承载第二种媒介。

**包含**

- 本地漫画目录或受支持的图片压缩包导入；
- 漫画详情、章节和图片顺序管理；
- 纵向滚动和横向翻页；
- 图片预加载、缓存和清理；
- 漫画进度持久化和恢复。

**不包含**

- 在线漫画源、站点规则或内容聚合；
- 漫画云端下载和番剧播放。

**完成标准**

- 本地漫画导入、阅读、退出和恢复闭环可重复验证；
- 损坏图片、顺序异常、文件丢失和缓存清理行为符合漫画规格；
- 新媒介复用媒体库、收藏和进度基础逻辑。

### 4.4 Version 0.4：本地媒体管理与阅读体验

**进入条件**

小说和漫画的本地闭环已经发布，统一媒体模型能够支持更完整的查找、组织和维护能力。

**包含**

- 跨小说和漫画的搜索、筛选与排序；
- 用户标签、自定义书架与收藏组织；
- 本地阅读历史和基础统计；
- 本地元数据与封面编辑；
- 应用数据的导出、备份、校验和恢复；
- 漫画页码、缩放、裁边等阅读体验改进；
- Android 普通图片目录持久访问的可行性验证。

**不包含**

- 账号、服务器和跨设备同步；
- 在线内容源、站点规则和下载；
- 推荐、社区和商业化。

**完成标准**

- 大型混合媒体库能够快速查找、筛选和稳定排序；
- 标签、自定义书架、历史和元数据编辑均可持久化并安全迁移；
- 备份可校验，恢复失败不得破坏现有数据；
- 阅读体验改动不回归 TXT、EPUB 与漫画核心路径；
- Windows 与 Android 通过自动化和人工验收。

### 4.5 Version 0.5：在线资源与插件框架

**进入条件**

本地媒体管理稳定，在线输入的权限、合规、失效和隔离模型已经形成书面规格。

**包含**

- 可安装、启停、升级和删除的内容源插件；
- 搜索、详情、目录和内容获取的统一只读接口；
- 插件域名权限声明、请求限速、超时、并发与缓存边界；
- Cookie/凭据按插件隔离，不进入日志或导出；
- 无版权测试源和用户主动导入规则；
- 下载与离线缓存必须在在线浏览闭环稳定后单独进入任务。

**不包含**

- 官方仓库内置侵权站点规则；
- 绕过登录、付费、DRM、验证码或站点访问控制；
- 未经用户确认向第三方发送本地媒体、历史或账号数据；
- 番剧播放、推荐、社区和商业化。

**完成标准**

- 插件权限在安装前可见，运行时只能访问已声明域名与能力；
- 单个插件故障、恶意响应或失效规则不会破坏本地媒体库；
- 规则安装、升级、停用和删除有可重复测试；
- 缓存与凭据清理边界明确，日志不泄露敏感内容；
- 发布仓库只提供合法、可再分发的测试证据。

### 4.6 Version 0.6：账号与同步

**进入条件**

本地实体、修改与删除语义稳定，Version 0.4 的备份恢复已验证，且同步协议完成复核。

**包含**

- Go API 和 PostgreSQL；
- 注册、登录、Token 生命周期和设备标识；
- 收藏、阅读进度、书签、标签和阅读设置同步；
- 首次全量与后续增量同步；
- 幂等、删除传播、冲突处理、重试和恢复；
- 设备撤销和最低隐私、安全要求。

**不包含**

- 云端原始内容托管；
- 推荐、社区和商业化；
- 将在线内容源凭据混入同步数据。

**完成标准**

- 两台设备能在离线修改后恢复同步；
- 重复请求不会重复应用操作；
- 删除、游标失效和冲突场景有自动化测试；
- 同步失败不破坏本地可用数据；
- 协议符合 `docs/sync-protocol.md`。

### 4.7 后续候选方向

以下能力没有排期，必须重新评估用户价值、维护成本和合规风险后才能进入正式版本：

- 番剧播放；
- macOS、iOS、Linux 和 Web 正式支持；
- Rust 解析、图片或下载模块；
- AI、推荐和社区。

## 5. 统一开发顺序

1. 工程骨架；
2. 本地数据模型与媒体库；
3. TXT 导入与小说阅读；
4. EPUB、Android 与小说体验打磨；
5. 本地漫画；
6. 本地媒体管理与阅读体验；
7. 在线资源与插件框架；
8. 账号与同步；
9. 重新评估后的候选能力。

后续模块不得以“以后会用到”为理由提前引入大规模基础设施。

## 6. 平台策略

- MVP 0.1：Windows 是唯一承诺支持的平台。
- MVP 0.2：增加 Android。
- macOS、iOS、Linux、Web：长期兼容目标，不属于早期承诺。
- 每个正式支持平台必须具备构建步骤、基本测试、已知限制和发布检查。

仓库保留 Flutter 生成的多端目录，不代表这些平台已经获得产品级支持。

## 7. 技术演进

### 7.1 客户端

客户端使用 Flutter 和 Dart。状态管理、路由和本地数据库的正式选择通过架构决策记录确定。

### 7.2 服务端

Go 和 PostgreSQL 从 Version 0.6 开始承担账号、设备和同步。在此之前不为了展示全栈而建立空服务。

### 7.3 Rust

Rust 是可选性能层，不是固定交付物。只有同时满足以下条件才引入：

1. 已有可重复基准；
2. Dart 未达到已冻结指标；
3. 已评估算法、I/O 和内存优化仍不足；
4. FFI 的构建、测试和跨平台维护成本可接受。

## 8. 成功标准

### 8.1 产品价值

- 作者能用发布版本完成真实阅读，而不只是演示页面；
- 每个版本至少有一条稳定、可重复的核心演示路径；
- 异常不静默丢失进度或导入记录。

### 8.2 工程质量

- 关键数据规则和恢复路径有自动化测试；
- 支持平台能从干净环境按文档构建；
- 性能结论记录样本、机器、版本、运行次数和结果；
- 关键架构选择记录备选方案和代价。

### 8.3 GitHub 与求职展示

- README 区分已实现、开发中和计划能力；
- 发布版本包含演示、安装步骤、变更记录和已知问题；
- 至少能说明一个数据一致性、性能或异常恢复问题；
- 不用未落地的 Go、Rust 或多平台规划包装成熟度。

## 9. 范围变更规则

新需求必须回答：

1. 它解决当前版本核心演示路径中的什么问题？
2. 不做它是否仍能达到当前版本完成标准？
3. 它增加哪些依赖、测试和平台维护成本？
4. 是否涉及内容版权、隐私或第三方许可证？

如果不做仍能完成当前版本，默认移入候选方向。范围变化时先修改本文件，再更新任务清单和专题规格。

## 10. 主要风险

| 风险 | 表现 | 应对 |
|---|---|---|
| 范围膨胀 | 多条媒介和多平台同时推进 | 当前版本只保留一条核心增量 |
| 技术栈展示冲动 | 提前引入 Go、Rust 和复杂分层 | 技术必须对应需求、基准或 ADR |
| 数据丢失 | 文件变化、迁移或异常终止破坏状态 | 明确事务、迁移、恢复和测试 |
| 内容与许可证 | 在线源、缓存、下载带来合规问题 | 早期只处理用户本地内容 |
| 文档失真 | README 宣称未实现能力 | 发布时检查状态，计划能力明确标注 |
| 单人维护中断 | 远期任务过细、状态难恢复 | 只详细拆当前版本，使用稳定任务 ID |

## 11. 文档与执行入口

- 当前任务：`docs/m03-task-list.md`
- 技术架构：`docs/tech-architecture.md`
- 本地数据：`docs/data-model.md`
- 小说规格：`docs/novel-reader-spec.md`
- 漫画规格：`docs/manga-reader-spec.md`
- 同步协议：`docs/sync-protocol.md`
- 质量策略：`docs/quality-strategy.md`
- 发布检查：`docs/release-checklist.md`
