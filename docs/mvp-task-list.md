# mirascope MVP 执行清单

## 1. 文档职责

本文是当前开发任务和状态的单一信息源。版本范围以 `plan.md` 为准，实体和行为以专题规格为准。

当前执行版本：**MVP 0.1 — Windows 本地 TXT 阅读闭环**。

## 2. 状态与完成规则

- `planned`：范围明确，尚未开始；
- `in_progress`：正在实施；
- `blocked`：存在明确阻塞，并记录原因；
- `done`：交付物存在，验收已经执行并留下证据。

任务字段：

- **优先级**：P0 是版本必需，P1 可在不影响主链路时延后；
- **依赖**：必须先完成的任务 ID；
- **交付物**：具体代码、文档或测试；
- **验收**：可执行或可观察的完成条件。

不能以“代码写完”“看起来可用”直接标记 `done`。

## 3. MVP 0.1 任务

### M01-001 工程基础决策

- 优先级：P0
- 状态：done
- 依赖：无
- 交付物：状态管理、路由、本地数据库和仓库结构的四份 ADR
- 验收：每份 ADR 包含备选方案、决策、代价和复核条件
- 证据：提交 `a91ae3e`；四份 ADR 已逐项覆盖备选方案、决策、代价和复核条件。

### M01-002 Flutter 应用骨架

- 优先级：P0
- 状态：done
- 依赖：M01-001
- 交付物：`app`、`core`、`shared`、`library`、`novel` 基础结构，替换默认计数器页面
- 验收：Windows 启动后可进入媒体库和设置页；默认示例代码和测试已移除
- 证据：提交 `6c71b5e`、`5c52618`、`e2ddaab`、`64b8882`、`c834a2d`；`flutter analyze --no-pub` 无问题，`flutter test --no-pub` 共 12 项通过，默认计数器标识扫描结果为空。
- Windows 实机证据：2026-07-30 本机 `flutter run -d windows` 在 26.2 秒内完成 debug 构建并启动真实窗口；媒体库、导入入口、设置和后续核心阅读验收均可进入。release 构建挂起作为 M01-018 发布阻塞单独保留，不再阻塞应用骨架完成。

### M01-003 路由、主题与应用启动

- 优先级：P0
- 状态：done
- 依赖：M01-002
- 交付物：路由配置、浅色/深色主题、启动初始化和依赖装配
- 验收：媒体库、小说详情、阅读器、设置之间可按预期导航；启动失败有可见错误
- 证据：提交 `e2ddaab`、`64b8882`、`c834a2d`；组件测试覆盖媒体库、设置及返回、小说详情、阅读器、未知路由、非法媒体 ID、安全启动失败页和日志脱敏，`flutter test --no-pub` 共 12 项通过。
- Windows 实机证据：真实窗口中已完成媒体库、详情、阅读器及返回书架导航；实机发现的返回入口缺失和返回后操作状态不释放均已修复并复验，中文字体已采用常见系统字体及跨平台回退链。

### M01-004 日志与统一错误模型

- 优先级：P0
- 状态：done
- 依赖：M01-002
- 交付物：结构化错误类型、日志封装和用户错误映射
- 验收：日志不记录正文和完整敏感路径；小说规格中的导入错误有稳定代码
- 证据：设计与实施计划提交 `20adb17`；实现提交 `a65c507`。
- 行为证据：MVP 0.1 启动、媒体库和 TXT 导入错误代码集中定义且唯一；未知异常只映射为安全兜底错误；结构化日志 API 只接受稳定代码、固定阶段、文件大小和耗时，不接受路径、标题、正文、原始异常或堆栈。
- 验证：`dart format .` 检查 81 个文件且 0 变更；`flutter analyze --no-pub` 无问题；`flutter test --no-pub` 共 109 项通过。
- 边界审计：现有启动和媒体库动作已迁移到安全日志封装；业务代码没有绕过封装直接写日志；数据库 schema 和用户原文件未修改。

### M01-005 本地数据库与迁移

- 优先级：P0
- 状态：done
- 依赖：M01-001、M01-002
- 交付物：MVP 0.1 所需表、约束、Repository 和 schema 迁移
- 验收：空库初始化、当前 schema 对已提交 v1 快照的严格验证和失败回滚测试通过；模型符合 `docs/data-model.md`
- 证据：实现提交 `d91417f`、`ea2ae04`、`169c9d4`、`fd3ffee`、`6a1b10f`、`b9c0612`、`3e629ad`、`de07469`、`e6845d2`、`d85e117`、`69c15af`、`b329085`、`36c7dbe`。
- 验证：`dart run build_runner build` 成功；`dart format .` 检查 64 个文件且 0 变更；`flutter analyze --no-pub` 无问题；`flutter test --no-pub` 共 67 项通过。
- schema 证据：全新当前 `AppDatabase.onCreate` 与已提交 v1 helper 严格一致；测试覆盖 6 张表、6 条级联外键、4 个声明索引、故意失败事务回滚，以及跨作品章节进度拒绝且不写入；重复执行 `schema dump` 与 `schema generate` 后对应 `git diff --exit-code` 均为 0。
- 边界审计：domain/presentation 无 Drift import；普通日志仅记录稳定代码，不包含原始异常、堆栈或完整源路径；schema v1 仅含批准的 6 类实体，不含 Bookmark。

### M01-006 媒体库

- 优先级：P0
- 状态：done
- 依赖：M01-005
- 交付物：媒体库列表、空状态、最近打开排序、归档/恢复和安全永久删除
- 验收：新增、重启、归档、恢复和永久删除后状态一致；不会删除用户原文件
- 证据：设计与计划提交 `2cbee0b`、`eb527da`、`751e793`；实现与验收提交 `f4d4711`、`1f66203`、`48ef31c`、`ff3b44d`、`4c602c1`、`634213a`、`7b1f8d5`。
- 行为证据：活动与归档列表均由 Repository 响应流驱动；打开作品先持久化 UTC 时间；归档可撤销，归档页可恢复；Provider 重建后排序保持；媒体记录与导入源路径均未删除。
- 实机修复：打开作品时先完成时间记录并释放按媒体 ID 的忙碌状态，再执行页面跳转，修复从阅读页返回后“更多操作”持续显示加载动画。
- 界面证据：覆盖 loading、empty、error、data 四类状态，600/900/1440 像素自适应网格、长标题、键盘语义、浅色与深色 Material 3 主题均有 Widget 测试。
- 验证：`dart run build_runner build` 成功；`dart format .` 无未格式化文件；`flutter analyze --no-pub` 无问题；`flutter test --no-pub` 共 103 项通过；schema dump/generate 重复生成后零差异。
- 实机修复：人工验收发现归档后没有永久删除入口，导致完成记录持续参与重复检测、无法重新导入解析。现仅在归档页提供二次确认永久删除，级联移除媒体、媒体库条目、章节、进度、作品级设置和导入记录，再清理派生正文；原始 TXT 始终不删除。
- 删除验证：活动作品拒绝永久删除；确认文案明确不可撤销和不删除原文件；删除成功后完成导入记录不存在，相同文件可重新进入导入解析。格式检查覆盖 134 个文件且 0 变更，静态分析无问题，全量测试 209 项通过。
- 边界审计：domain/presentation 无 Drift import；永久删除只从归档页触发；数据库聚合先安全删除，派生正文随后尽力清理；普通日志仅记录稳定代码，不包含标题、路径、原始异常或堆栈。

### M01-007 TXT 文件选择与指纹

- 优先级：P0
- 状态：done
- 依赖：M01-004、M01-005
- 交付物：Windows 文件选择、文件校验、指纹和重复检测
- 验收：不存在、无权限、空文件、重复文件和同名不同内容得到规格规定的结果
- 证据：设计与计划提交 `50d2448`；核心实现提交 `2cd6c7d`；生产装配与边界测试提交 `d4a9a26`。
- 行为证据：Flutter 官方 `file_selector` 只选择单个 TXT；文件检查使用流式 SHA-256 和字节数形成稳定指纹；读取前后复查文件大小和修改时间；已完成记录返回已有媒体 ID，失败或缺失记录不造成误判；取消选择不是错误。
- 验证：`dart run build_runner build` 成功；`dart format .` 检查 90 个文件；`flutter analyze --no-pub` 无问题；`flutter test --no-pub` 共 122 项通过；数据库 schema 无内容变化。
- 边界审计：领域与应用层不依赖文件选择插件或 Drift；日志不记录路径、文件名、正文、原始异常或堆栈；本阶段不写导入实体、不修改用户原文件，也不展示无法继续的半成品导入按钮。
- 阻塞：M01-002 记录的本机 MSVC 宿主工具链异常仍阻止 Windows 应用实际启动，因此尚未完成原生选择器的人工点击验收，不能标记为 `done`；已实现部分可继续供 M01-008 使用。

### M01-008 TXT 编码与规范化

- 优先级：P0
- 状态：done
- 依赖：M01-007
- 交付物：UTF-8、UTF-16、GB18030 识别、用户选择编码和文本规范化
- 验收：固定样例测试通过；无法可靠识别时进入预览选择，不静默乱码
- 证据：设计与计划提交 `832b197`；GB18030 平台依赖提交 `b0feb48`；编码规则实现提交 `6964763`；文件读取与生产装配提交 `e4b46bb`。
- 行为证据：按 BOM、严格 UTF-8、GB18030 字节结构的固定顺序识别；UTF-16 LE/BE 严格验证代理项；未知编码要求用户选择；手动选择失败可重试；预览按 Unicode 码点截取；CRLF/CR 统一为 LF，同时保留段落、繁简、全角和标点。
- 文件证据：解码前后核对 M01-007 候选的大小和修改时间，变化时返回 `source_changed`；读取不修改用户原文件；平台 GB18030 转换通过领域接口隔离。
- 验证：`dart run build_runner build` 成功；`dart format .` 检查 100 个文件且 0 变更；`flutter analyze --no-pub` 无问题；`flutter test --no-pub` 共 138 项通过。
- 边界审计：自动化测试使用自建字节样例；日志不记录路径、原始字节或正文；本阶段不写数据库；实际编码由成功结果携带，M01-010 已记录 `text_encoding` 字段和 schema 迁移前置约束。
- Windows 完成证据：2026-07-30 使用内容一致的无版权 UTF-8、UTF-16 LE 和 GB18030 三份样例在真实候选版中分别导入；书架均正常创建作品，中文标题与正文无乱码，UTF-16 与 GB18030 平台转换通过。

### M01-009 章节识别

- 优先级：P0
- 状态：done
- 依赖：M01-008
- 交付物：章节解析器、样例集和无章节回退
- 验收：`docs/novel-reader-spec.md` 中的中英文章节样例稳定通过；无章节文件生成单一正文单元
- 证据：设计与计划文档、领域结果和纯应用层解析器已实现；表驱动测试覆盖规格列出的 7 种中英文格式，以及英文大小写、行内误命中、80 码点上限、连续空标题、序言、UTF-16 字符偏移、顺序与全文连续覆盖、无章节回退。
- 实机修复：Windows 验收发现轻小说常见的 `▶︎▶︎【【第一话】 标题】`、`闲话`、`最终话`、`尾声` 和 `后记` 未被识别；现已安全剥离装饰括号并扩展标题规则，保留原始 UTF-16 源偏移。
- 验证：`dart run build_runner build` 成功；`dart format .` 检查 134 个文件且 0 变更；`flutter analyze --no-pub` 无问题；`flutter test --no-pub` 共 206 项通过。
- 边界审计：解析输入类型为 M01-008 的 `DecodedTxt`；本阶段不读取文件、不写数据库、不调用平台插件、不修改原文，也不生成持久化实体。

### M01-010 原子导入

- 优先级：P0
- 状态：done
- 依赖：M01-005、M01-007、M01-008、M01-009
- 交付物：协调文件、解析、派生数据和数据库的导入用例
- 验收：成功导入形成一致实体；任一步失败回滚半完成数据并保留失败记录
- 前置约束：原子写入前为 `ImportRecord` 增加明确的 `textEncoding`/`text_encoding` 字段及 schema 迁移，用于持久化 M01-008 的实际解码编码；不得借用现有无关字段。
- 证据：schema v2 已加入 `text_encoding` 并允许失败记录在媒体回滚后不持有外键；v1 升级不猜测历史编码。完整用例已串联重复检查、M01-008 解码、M01-009 章节识别、章节哈希与定位、UTF-8 派生文件暂存/提升、数据库聚合事务和失败补偿。
- 行为证据：成功 TXT 强制保存实际编码和媒体 ID；派生文件提升失败会回滚媒体、媒体库、章节和成功记录，并另存无媒体外键的稳定失败记录；路径不进入内容引用，临时与最终清理幂等。
- 验证：`dart run build_runner build` 成功；`dart format .` 检查 109 个文件；`flutter analyze --no-pub` 无问题；`flutter test --no-pub` 共 161 项通过；schema v1→v2 迁移和当前 v2 严格验证通过。
- Windows 完成证据：真实文件选择、UTF-8/UTF-16/GB18030 解码、章节解析、派生文件读取、媒体库刷新和重复导入均已在本机及全新 Windows Sandbox 候选版中完成；失败回滚继续由自动化测试覆盖。
- 界面接入：M01-018 候选审计发现媒体库缺少导入入口，现已接入文件准备与原子导入用例；支持书名确认、显式编码选择、重复文件打开、安全失败反馈、成功后刷新并打开详情，以及流程期间防重复点击。

### M01-011 小说详情与目录

- 优先级：P0
- 状态：done
- 依赖：M01-006、M01-010
- 交付物：小说详情、章节目录和打开阅读入口
- 验收：章节顺序稳定、当前章节明确、源文件丢失时提供重新定位
- 证据：已实现小说详情聚合、Drift 查询仓库、按媒体 ID 的 Riverpod 状态、详情页和阅读路由入口；目录按 `orderIndex` 稳定排序，源 TXT 不可用时保留作品和目录并显示重新定位入口。
- 验证：`dart run build_runner build` 成功；`dart format .` 检查 115 个文件且 0 变更；`flutter analyze --no-pub` 无问题；`flutter test --no-pub` 共 165 项通过。数据层测试覆盖章节顺序、来源检查和缺失媒体；Widget/路由测试覆盖正常详情、阅读入口和源文件缺失提示。
- 边界审计：presentation 不直接查询 Drift 或文件系统；完整源路径不进入界面和日志；重新定位按钮仅表达 M01-015 的入口，不在本阶段静默替换来源。
- Windows 实机证据：真实 UTF-8 TXT 已完成详情、目录和阅读入口验收；装饰型“第X话”等标题经修复后可形成多章节，来源丢失时仍保留目录、重新定位提示和派生正文阅读入口。实际重新定位流程继续由 M01-015 跟踪。
- 目录定向修复：详情页目录现在将所选内容单元 ID 写入阅读路由；阅读控制器优先打开用户明确选择的章节并从章首记录进度，不再被旧阅读进度覆盖。

### M01-012 纵向滚动阅读器

- 优先级：P0
- 状态：done
- 依赖：M01-003、M01-011
- 交付物：正文、目录、章节导航和桌面键鼠交互
- 验收：第一章、最后一章和读取失败行为正确；窗口变化后内容仍可用
- 证据：已实现规范化派生文本读取仓库、阅读状态控制器和纵向滚动界面；支持目录、上一章/下一章、左右方向键、PageUp/PageDown、选中文本和桌面宽度约束。
- 数据证据：内容引用限制在安全的 `content/*.txt`；派生文件严格 UTF-8 解码；`txt-v1` UTF-16 偏移经过范围验证；每章读取前复算 SHA-256，路径穿越、非法定位和哈希不符均拒绝。
- 行为证据：首章禁用上一章、末章禁用下一章；成功切章回到顶部；切章读取失败保留当前章节正文和章节索引，并保留失败目标供重试。
- 键盘修复：左右键继续切换上一章/下一章，PageUp/PageDown 保持章节导航；上下方向键现在平滑滚动当前正文，并限制在有效滚动范围。
- 验证：`dart run build_runner build` 成功；`dart format .` 检查 122 个文件且 0 变更；`flutter analyze --no-pub` 无问题；`flutter test --no-pub` 共 171 项通过。
- Windows 完成证据：2026-07-30 已复验左右方向键切换章节、上下方向键滚动正文、PageUp/PageDown 章节导航、鼠标滚动、长章节和窗口缩放，未发现问题。

### M01-013 阅读设置

- 优先级：P0
- 状态：blocked
- 依赖：M01-005、M01-012
- 交付物：字号、行距、浅色、深色和护眼主题
- 验收：设置即时生效并在重启后恢复；无效配置安全回退
- 证据：已实现全局与作品级阅读设置控制器、设置页和阅读器 BottomSheet；支持 12–36 字号、1.2–2.4 行距、跟随系统、浅色、深色和护眼主题，作品设置按字段覆盖全局值。
- 行为证据：调整先即时更新正文预览，再按顺序写入 `ReaderPreference`；保存失败恢复上一个有效值；恢复默认重新继承上层设置；Provider 重建后从数据库恢复。
- 安全证据：保存拒绝非有限值、越界字号/行距和未知主题；数据库中已有的损坏字段按字段回退到全局或程序默认值，不阻断阅读。
- 验证：`dart run build_runner build` 成功；`dart format .` 检查 127 个文件且 0 变更；`flutter analyze --no-pub` 无问题；`flutter test --no-pub` 共 177 项通过。
- 阻塞：Windows MSVC 工具链仍阻止应用真实启动，尚未完成真实窗口中的滑块、四主题视觉、窄窗口布局和重启恢复人工验收；完成前不能标记为 `done`。

### M01-014 进度持久化与恢复

- 优先级：P0
- 状态：done
- 依赖：M01-005、M01-012
- 交付物：语义定位、节流写入、revision 保护和恢复流程
- 验收：正常关闭和异常终止后恢复；旧异步写入不能覆盖新位置；排版变化后不依赖像素恢复
- 证据：阅读器以章节 ID 和 `char-v1:<UTF-16 offset>` 保存语义位置，启动时恢复对应章节和字符位置；章节缺失或定位损坏时安全回退到首章或有效比例。
- 行为证据：滚动位置采用 750ms 尾随防抖且仅写入最新值；写入串行化并递增 revision，冲突时重新加载数据库版本后重试，避免旧异步结果覆盖较新进度。
- 生命周期证据：切换章节、应用进入后台、页面销毁和控制器关闭时主动刷新待写进度；章节切换先保存旧章节，再立即记录新章节起点。
- Windows 人工证据：本机 debug 构建与启动成功；正常关闭恢复、快速滚动仅保留最新位置、排版变化后的语义恢复均通过。阅读页现提供固定“返回书架”入口，返回前刷新待写进度，并保留独立目录按钮。
- 验证：`dart run build_runner build` 成功；`dart format .` 检查 134 个文件且 0 变更；`flutter analyze --no-pub` 无问题；`flutter test --no-pub` 共 211 项通过。
- Windows 完成证据：正常关闭、任务管理器异常终止、快速滚动最新位置、排版变化语义恢复均由真实窗口确认；修复后多章节样例可解析和切换，详情目录也能从指定章节章首进入。

### M01-015 文件变化与重新定位

- 优先级：P0
- 状态：blocked
- 依赖：M01-010、M01-014
- 交付物：文件缺失状态、重新定位和源文件变化确认
- 验收：文件移动后保留媒体与进度；指纹不一致时不静默替换
- 证据：交接同步、设计与计划提交 `4e1be20`；来源仓储、重新定位用例和详情页接入提交 `72d4a4f`；稳定 `source_changed` 确认语义提交 `d032ad2`。
- 行为证据：详情发现来源不存在时将有效导入记录条件标记为 `missing`，但保留媒体、章节、派生文本和进度；重新选择同指纹文件后原子更新路径、大小和修改时间并恢复 `completed`；取消选择和不同指纹均不写数据库。
- 并发证据：路径更新同时校验导入 ID、媒体 ID、旧指纹和允许状态，影响行数不为 1 时失败；同一详情页操作期间按钮禁用，成功后刷新详情。
- 变化保护：不同指纹返回明确的 `source_changed` 确认状态，界面说明当前可读版本和进度已保留，不执行删除后重导入；确认后的原位重解析必须在能够先生成新版本、映射进度并原子替换时另行接入。
- 实机修复：人工验收确认来源被标记为 `missing` 后详情页错误禁用了派生正文入口；现改为始终允许打开已有派生章节，同时保留重新定位提示和操作。
- Windows 人工证据：2026-07-30 已将原始 TXT 移动到新位置并选择同内容文件，作品与进度得到保留；另选不同内容文件时应用拒绝静默替换，原有作品和进度未被覆盖。
- 验证：格式检查覆盖 134 个文件且 0 变更；`flutter analyze --no-pub` 无问题；`flutter test --no-pub` 共 206 项通过。
- 阻塞：来源丢失后的派生正文阅读、同指纹重新定位及不同指纹零写入保护均已通过实机验收；不同内容确认后的安全原位重解析尚未实现，当前只提供零写入保护门槛，因此不能标记为 `done`。

### M01-016 自动化质量门槛

- 优先级：P0
- 状态：done
- 依赖：M01-003 至 M01-015
- 交付物：单元、数据层、Widget 测试和检查命令
- 验收：`docs/quality-strategy.md` 的 MVP 0.1 合并门槛全部取得当次结果
- 实施：已新增 GitHub Actions，分别执行格式、静态分析、全量测试和 Windows release 构建；Flutter 固定为 `3.44.8`，并限制工作流为只读权限。
- 本地证据：格式检查覆盖 130 个文件且 0 变更；`flutter analyze --no-pub` 无问题；`flutter test --no-pub` 共 189 项通过。
- 远程证据：提交 `f3cb39c` 的 GitHub Actions 运行 `30452521276` 全部通过；通用质量作业耗时 2 分 14 秒，Windows release 构建耗时 4 分 37 秒。
- 本机限制：本地 Windows 构建仍会停在 MSVC/CMake 编译阶段；远程成功证明项目可在干净 Windows runner 构建，但不替代本机修复和核心路径人工验收。

### M01-017 性能基线

- 优先级：P1
- 状态：done
- 依赖：M01-010、M01-012
- 交付物：小型、常规和 50 MB TXT 的导入、打开和内存记录
- 验收：记录机器、版本、构建模式、至少三次运行及中位数，不虚构阈值
- 方案：使用生成式无版权样本、独立子进程和 Dart AOT 构建，分别记录完整导入、首次打开、章节切换及 RSS；每组预热一次、正式运行三次。
- 证据：基准工具实现提交 `bbfe0f4`；完整报告见 `docs/performance/2026-07-29-txt-baseline.md` 和同名 JSON。
- 环境：Windows 10 19045、Intel Core i5-12450H、12 逻辑处理器、16088.49 MiB 内存、Flutter 3.44.8、Dart 3.12.2、Dart AOT bundle。
- 中位数：100 KiB 为导入 12.697 ms、打开 11.840 ms、RSS 增量 4.17 MiB；5 MiB 为导入 306.800 ms、打开 8.716 ms、RSS 增量 29.28 MiB；50 MiB 为导入 2808.843 ms、打开 61.365 ms、RSS 增量 209.52 MiB。
- 限制：这是核心数据链路的首次基线，不包含选择器、窗口和页面首帧；RSS 包含运行时与 SQLite，尚未冻结通过阈值。

### M01-018 演示与候选发布

- 优先级：P0
- 状态：in_progress
- 依赖：M01-015、M01-016、M01-017
- 交付物：Windows 候选构建、运行说明、截图或短演示、已知问题
- 验收：按 `docs/release-checklist.md` 完成检查；从干净环境复现核心演示路径
- 发布材料：版本已校准为 `0.1.0+1`；README 已按真实状态重写；演示指南、已知问题和 `0.1.0-rc.1` 候选检查记录已经建立。
- 功能修复：媒体库已接入“导入 TXT”入口，复用现有文件准备与原子导入用例；书名对话框改为自行持有并在路由完全销毁时释放输入控制器，修复 Windows 实机上的释放后使用异常。
- 实机缺陷修复：补齐阅读页返回书架与目录入口、来源丢失后的派生正文阅读，以及装饰型轻小说章节标题解析。
- 体验修复：全局中文字体优先使用 Windows 常见的“Microsoft YaHei UI”，并配置 Microsoft YaHei、Noto Sans CJK SC、Noto Sans SC、PingFang SC 和系统无衬线字体回退；打开作品的忙碌状态在导航前释放。
- 删除闭环：归档页新增永久删除与二次确认，只删除应用数据和派生正文，不删除原始 TXT；删除完成记录后允许相同文件重新导入和重新解析。
- 验证：格式检查覆盖 134 个文件且 0 变更；代码生成成功；静态分析无问题；全量测试共 211 项通过。
- 边界：远程 Windows 构建通过不等于本机安装和交互验证通过；未取得真实截图前不使用设计稿或测试图冒充发布截图。
- 本机 release 证据：2026-07-30 再次执行 `flutter build windows --release --no-pub`，持续无输出；本轮 `cmake` 与 `cl` 进程 CPU 均为 0，确认仍卡在本机 MSVC 原生编译阶段后终止。debug 构建与真实窗口运行正常。
- 干净环境证据：提交 `62d89b5` 的首个候选 ZIP 在全新 Windows Sandbox 启动时因缺少 `MSVCP140.dll` 失败；提交 `3d182ca` 改为从构建工具链的 `Microsoft.VC143.CRT` 目录加入可再分发的 x64 Visual C++ 运行库。修复后 Actions 运行 `30515324831` 全部通过，14.29 MB 构建物 SHA-256 为 `e7e79f9e55f81a1a90408019e2982bbe50561c5dbbdfc8dba8cb598e40e45d3b`。
- Windows Sandbox 复验：2026-07-30 在未预装 Visual C++ 运行库的全新沙盒中成功启动并显示空书架；随后完成真实 TXT 导入、章节解析、详情指定章节进入、上下滚动、左右换章、返回书架，以及关闭应用后重新启动恢复进度，未发现问题。
- 发布截图：真实候选版已使用无版权样例拍摄媒体库、小说详情目录和阅读设置三张截图，保存于 `docs/assets/screenshots/0.1.0-rc.1/` 并接入 README。
- 许可证收口：项目采用 MIT License；直接运行依赖已核对为兼容的宽松许可证；Windows ZIP 会附带项目许可证、第三方概览及从实际 Flutter 构建生成的完整许可证正文。撤回的传递依赖 `jni 1.0.1` 已升级到 `1.0.2`，格式、静态分析和 211 项测试通过。
- 候选数据证据：2026-08-04 在全新 Windows Sandbox 中完成由已提交 schema v1 helper 合成的数据升级，作品、章节和派生正文均保留；同时通过归档、撤销、归档页恢复及恢复后阅读。
- 备份恢复证据：2026-08-04 在全新 Windows Sandbox 中关闭应用后备份整个应用支持目录；移走原目录并启动后得到全新空书架；再次关闭，移除自动新建的空目录并恢复备份后，作品、章节和派生正文均恢复成功。
- 发布结果：2026-08-04 创建并推送 `v0.1.0`，标签构建全部通过；GitHub Release 已发布并附带经过 SHA-256 校验的 Windows 压缩包。本机 release 构建仍受 MSVC 环境问题影响，但不影响已验证的 CI 正式构建。

## 4. MVP 0.1 验收摘要

- [x] Windows 上完成 TXT 导入到恢复进度的核心路径
- [x] 支持编码和异常文件行为符合小说规格
- [x] 重复导入、文件移动、数据库失败不造成数据丢失
- [x] 自动化检查和 Windows 构建取得当次通过证据
- [x] 性能基线、运行说明、演示和已知问题齐全

## 5. 后续版本门槛

### MVP 0.2

- 进入：MVP 0.1 已发布且不存在阻塞阅读或数据丢失问题
- 退出：Windows/Android 小说闭环、EPUB、书签、性能阈值和公开 Release 完成
- 规格：`plan.md`、`docs/novel-reader-spec.md`、`docs/quality-strategy.md`

### Version 0.3

- 进入：小说阅读器稳定，统一媒体模型经实际实现验证
- 退出：本地漫画导入、两种阅读模式、缓存和进度恢复完成
- 规格：`plan.md`、`docs/manga-reader-spec.md`
- 当前执行清单：`docs/m03-task-list.md`

### Version 0.4

- 进入：小说与漫画本地闭环已发布
- 退出：本地搜索、筛选、组织、历史、备份恢复和阅读体验通过双平台验证
- 规格：`plan.md`、`docs/m04-task-list.md`

### Version 0.5

- 进入：在线插件权限、合规、缓存和失效模型完成评审
- 退出：合法测试源上的插件安装、权限、浏览和故障隔离闭环完成
- 规格：`plan.md`

### Version 0.6

- 进入：本地修改、迁移和删除语义稳定
- 退出：两台设备的增量同步、幂等、删除、冲突和恢复验证完成
- 规格：`plan.md`、`docs/sync-protocol.md`

后续版本临近实施时另行拆分任务，不在当前清单维护伪精确的远期待办。
