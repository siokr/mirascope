# mirascope 0.3.0-rc.1 候选记录

> 状态：准备中，尚未创建候选标签或正式 Release。

## 范围

- [x] 本地漫画目录与 ZIP/CBZ 的安全扫描、稳定指纹和重复检测；
- [x] 图片验证、自然排序、章节/页清单、缩略图和原子导入；
- [x] 漫画媒体库、详情、目录、纵向/横向阅读、页级进度与偏好；
- [x] 有界缓存、预加载取消、安全清理和异常恢复；
- [x] 漫画性能基线（85 章、891 页、243.60 MiB；三次正式运行完整扫描中位数 15.17 秒，冷缓存首张 6.02 ms，连续 50 页 463 ms）；
- [x] Windows 与 Android 人工验收；
- [ ] 正式构建、签名验证、哈希与干净环境复验；
- [ ] `v0.3.0` 标签和 GitHub Release。

## 版本与数据

- 应用版本：`0.3.0+3`；
- 数据库 schema：v5（v4 → v5 仅扩展漫画双页阅读模式，保留既有偏好）；
- Android application ID：`io.github.siokr.mirascope`；
- schema v5 可从 v1/v2/v3/v4 迁移并保留既有小说、进度、书签和偏好；
- 更新前仍建议备份应用支持目录，不支持将 v5 数据目录降级给旧版本。

## 自动化候选证据

- 候选提交：`b8212ed`；
- GitHub Actions #57，运行 `31870218254`；
- 格式检查、静态分析、376 项自动化测试、Windows release 和 Android debug APK 全部通过；
- 分支构建按设计跳过 Android 正式签名任务，正式签名只在 `v*` 标签运行；
- Windows artifact SHA-256：`d587a1101d1121a3df26ceea37960aac9977136a97896fa6fbf46762256f645d`；
- Android debug artifact SHA-256：`5ad422e10430a10ab5537ceafa2a7a6d2f7144bd596542cf4db587a0250397a4`。

## 已知限制

- Android 普通图片目录跨重启访问受当前文件选择插件限制，0.3 仅承诺 Android ZIP/CBZ 闭环；
- 不支持 RAR/7z、加密压缩包、动画图片语义或超预算容器；
- 不包含在线漫画源、站点规则、下载、番剧、账号或同步；
- Windows 暂无 MSI/MSIX，Android 暂未提供应用商店安装包。

## 发布决定

待性能报告、[双平台验收](../m03-cross-platform-acceptance.md)、CI 正式产物和签名复验完成后填写。当前不得描述为已发布版本。
