# mirascope 0.1.0-rc.1 候选记录

> 状态：准备中，尚未创建 Git 标签或 GitHub Release。

## 1. 候选范围

- [x] 版本范围与 `plan.md` 的 MVP 0.1 一致；
- [x] README 已按当前代码重写；
- [x] 非目标能力没有列为当前支持；
- [x] 已知问题和限制已记录；
- [x] Windows 核心演示路径人工通过；
- [x] 真实截图或短演示完成。

## 2. 版本与数据

- 版本：`0.1.0+1`；
- 数据库 schema：v2；
- [x] `pubspec.lock` 已提交；
- [x] 离线依赖解析成功且锁文件无变化；
- [x] 空库创建与 schema v1→v2 自动化迁移测试通过；
- [x] 事务失败回滚自动化测试通过；
- [ ] 在候选 Windows 应用上验证已有 v1 数据升级；
- [ ] 验证并记录生产候选数据备份与恢复；
- [ ] 完成许可证决策。

当前没有已发布生产版本，因此不声称支持从 v2 降级到旧 schema。

## 3. 自动化证据

M01-017 提交 `d0954a1`：

- [x] 格式检查通过；
- [x] 静态分析通过；
- [x] 193 项测试通过；
- [x] GitHub Windows release 构建通过；
- GitHub Actions：<https://github.com/siokr/mirascope/actions/runs/30457172195>。

M01-018 当前分支：

- [x] 本地格式检查覆盖 134 个 Dart 文件，0 变化；
- [x] 本地静态分析 0 问题；
- [x] 本地 211 项测试通过；
- [x] 当前候选提交的远程质量门禁；
- [x] 当前候选提交的 Windows release 构建。

提交 `3d182ca` 的 GitHub Actions 运行 `30515324831` 全部通过：质量作业用时 1 分 13 秒，Windows release 构建与运行库打包用时 3 分 32 秒。构建物 `mirascope-windows-0.1.0+1-x64` 为 14.29 MB，SHA-256 为 `e7e79f9e55f81a1a90408019e2982bbe50561c5dbbdfc8dba8cb598e40e45d3b`。

## 4. 性能

- [x] 使用固定生成式 100 KiB、5 MiB、50 MiB TXT；
- [x] 记录机器、构建模式、一次预热、三次正式运行和中位数；
- [x] 保存 JSON 原始结果和 Markdown 报告；
- [x] 解释 50 MiB 文件约 209.52 MiB RSS 增量；
- [ ] 与上一正式版本比较：不适用，这是首次基线。

报告：[`../performance/2026-07-29-txt-baseline.md`](../performance/2026-07-29-txt-baseline.md)。

## 5. Windows 人工验收

- [x] 空媒体库和导入入口；
- [x] UTF-8 导入、详情、目录和阅读；
- [x] UTF-16 与 GB18030；
- [x] 重复导入；
- [x] 正常关闭后恢复进度；
- [x] 异常终止后恢复进度；
- [ ] 归档、撤销和恢复；
- [x] 同指纹来源重新定位；
- [x] 不同指纹保护；
- [x] 键盘、鼠标、窗口缩放和长章节滚动；
- [x] 从干净环境启动或安装。

已确认本机 debug 构建与真实窗口可运行；实机发现并修复了导入对话框生命周期、阅读返回入口、来源丢失阅读、装饰型章节解析、永久删除和返回后忙碌状态问题。首个 CI 压缩包在全新 Windows Sandbox 中因缺少 `MSVCP140.dll` 无法启动，提交 `3d182ca` 补齐应用本地 Visual C++ 运行库后复验通过。沙盒内已完成空书架、UTF-8/UTF-16/GB18030 TXT 导入、章节解析、指定章节进入、键盘阅读、窗口缩放、返回书架和应用重启后进度恢复。

## 6. 发布物

- [x] README 包含源码运行步骤；
- [x] 演示指南存在；
- [x] 已知问题存在；
- [x] 候选 Windows 构建附件；
- [x] 候选附件 SHA-256；
- [x] 真实截图或短演示；
- [ ] LICENSE；
- [ ] 第三方许可证核对；
- [ ] 处理或明确豁免传递依赖 `jni 1.0.1` 已撤回问题；
- [ ] Git 标签；
- [ ] GitHub Release。

## 7. 变更摘要

首个候选版本包含：

- 本地 TXT 文件校验、编码、章节识别和原子导入；
- 媒体库、归档、小说详情、目录和阅读器；
- 阅读设置、语义进度和 revision 并发保护；
- 文件缺失检测与同内容重新定位；
- 数据库 schema v2 与迁移测试；
- GitHub Actions 自动质量门禁；
- TXT 性能基线工具和报告。

## 8. 发布决定

当前结论：**不可正式发布**。

需要先完成：

1. 许可证决策和第三方许可证核对；
2. 处理或明确豁免传递依赖 `jni 1.0.1` 已撤回问题。

真实候选截图：

- `docs/assets/screenshots/0.1.0-rc.1/01-library.png`；
- `docs/assets/screenshots/0.1.0-rc.1/02-novel-details.png`；
- `docs/assets/screenshots/0.1.0-rc.1/03-reader-settings.png`。
