# Version 0.3 任务清单

状态：`pending` / `in_progress` / `blocked` / `done`

## 本地漫画主线

- [x] `M03-001` 范围、依赖与可再分发样例（done：冻结目录、ZIP/CBZ、JPEG/PNG/WebP 边界；确认现有 `file_selector`、`archive` 与 Flutter 图片能力可进入实现且许可证兼容；Android 目录持久访问转入 M03-003 实机门槛；运行时生成自然排序、损坏图片、路径穿越、路径冲突和大展开样例）
- [x] `M03-002` 漫画领域模型与 schema v4（done：新增 `manga_pages` 固化页清单和 `manga_reader_preferences` 保存阅读模式/翻页方向；共享进度使用稳定 `page:<索引>:<页内比例>` locator；v1/v2/v3 直升 v4 并保留小说、书签、偏好和进度；静态分析及 303 项全量测试通过）
- [ ] `M03-003` Windows/Android 来源选择与身份（in_progress：ZIP/CBZ 与普通目录选择、签名校验、稳定流式指纹、重复检测和错误映射已实现，静态分析、312 项全量测试及 Android debug 构建通过；`file_selector_android 0.5.2+9` 不保留 SAF URI 权限且只转换主存储路径，Android 目录跨重启访问保持明确阻塞；来源丢失/重新定位待导入实体接通后完成）
- [x] `M03-004` 安全 ZIP/CBZ 容器与资源预算（done：按需读取且不整体解压；拒绝加密、路径穿越、绝对路径、重复/目录冲突、损坏中央目录、CRC 不符和不支持的压缩方式；限制 2 GiB 压缩包、20,000 条目、64 MiB 单条目、2 GiB 总展开量和 200 倍压缩比；20 项专项及 332 项全量测试通过）
- [x] `M03-005` 图片验证、自然排序与章节清单（done：使用 Flutter 图片编解码器真实解码并记录类型、像素尺寸、字节数和内容哈希；目录与 ZIP/CBZ 统一自然排序并固化章节/页清单；隐藏文件和非图片忽略，损坏图片计数并跳过，全损坏/空章节不生成；11 项新增专项及 343 项全量测试通过）
- [ ] `M03-006` 派生存储、缩略图与原子导入（pending：临时区生成清单和缩略图，事务写入媒体/书架/章节/导入记录；失败回滚数据库和派生文件，永久删除不触碰原文件）
- [ ] `M03-007` 媒体库、漫画详情与目录（pending：漫画卡片、封面、章节、页数、缺失来源和重新定位入口；小说功能不得回归）
- [ ] `M03-008` 漫画阅读器（pending：纵向连续滚动、横向单页、从左到右/从右到左；模式或窗口变化后保持对应页面；单张损坏允许继续）
- [ ] `M03-009` 页级进度与阅读偏好（pending：章节 ID、页索引和页内语义位置持久化；正常退出、异常终止、切章和模式切换恢复；旧异步写入不得覆盖新进度）
- [ ] `M03-010` 预加载与分层缓存（pending：相邻页优先、快速翻页取消无价值工作、内存/磁盘分别限额、缓存键包含来源变化标识；容量依据基准冻结）
- [ ] `M03-011` 缓存清理、来源恢复与异常闭环（pending：清缓存不删除媒体/进度/原文件；同指纹重新定位保留状态；整章不可读、内存不足和来源丢失有可恢复界面）
- [ ] `M03-012` 性能、双平台验收与 `v0.3.0` 发布（pending：记录大章节导入、首屏、连续阅读、内存与缓存基准；Windows/Android 核心闭环人工验收；正式构建、已知问题和 GitHub Release）

## 阶段门槛

- 只处理用户主动选择的本地内容，不加入在线漫画源、站点规则、下载或番剧；
- 不修改、移动或删除用户原始目录、压缩包和图片；
- 图片顺序必须持久化，不能依赖运行时文件系统遍历顺序；
- 不安全压缩包必须整体拒绝，导入失败不得留下半完成数据；
- 缓存、缩略图和可删除派生文件必须与原始来源明确分区；
- 每个 `done` 项必须留下新的自动化、构建、基准或人工验收证据；
- TXT/EPUB、书签、迁移、归档、重定位和双平台发布不得回归。
